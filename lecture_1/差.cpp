/* ===================================================
//----------------------------------------------------

// 读取四个整数 A,B,C,D，并计算 (A×B−C×D)的值。

//----------------------------------------------------
=================================================== */
#include<iostream>
using namespace std;
int Cha()
{
	int A, B, C, D;
	cin >> A >> B >> C >> D;
	cout << "DIFERENCA = " << (A * B) - (C * D) << endl;
	return 0;
}